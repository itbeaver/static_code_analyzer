require 'spec_helper'

describe StaticCodeAnalyzer do
  it 'has a version number' do
    expect(StaticCodeAnalyzer::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
